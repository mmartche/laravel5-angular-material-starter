ngDescribe({
    name: 'Test all-posts component',
    modules: 'app',
    element: '<all-posts></all-posts>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
