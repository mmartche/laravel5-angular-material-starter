ngDescribe({
    name: 'Test my-channels component',
    modules: 'app',
    element: '<my-channels></my-channels>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
